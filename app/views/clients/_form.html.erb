<%= form_with(model: client, local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sp_client_id %>
    <%= form.number_field :sp_client_id, id: :client_sp_client_id %>
  </div>

  <div class="field">
    <%= form.label :vi_score %>
    <%= form.number_field :vi_score, id: :client_vi_score %>
  </div>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :client_rank %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
