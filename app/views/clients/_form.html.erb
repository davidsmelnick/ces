<%= form_with(model: client, local: true) do |form| %>
  <% if client.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(client.errors.count, "error") %> prohibited this client from being saved:</h2>

      <ul>
      <% client.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :SPID %>
    <%= form.number_field :SPID, id: :client_SPID %>
  </div>

  <div class="field">
    <%= form.label :VIscore %>
    <%= form.number_field :VIscore, id: :client_VIscore %>
  </div>

  <div class="field">
    <%= form.label :rank %>
    <%= form.number_field :rank, id: :client_rank %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
