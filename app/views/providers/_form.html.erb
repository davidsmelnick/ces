<%= form_with(model: provider, local: true) do |form| %>
  <% if provider.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(provider.errors.count, "error") %> prohibited this provider from being saved:</h2>

      <ul>
      <% provider.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :sp_provider_id %>
    <%= form.number_field :sp_provider_id, id: :provider_sp_provider_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :provider_name %>
  </div>

  <div class="field">
    <%= form.label :sensitivity %>
    <%= form.text_field :sensitivity, id: :provider_sensitivity %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
